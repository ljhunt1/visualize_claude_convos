# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets
.env
.env.*

# Personal conversation data + derived artifacts (regenerable from scripts)
conversation_data/
corpus/
labels.jsonl
conv_vecs.npz
canonical_tags.jsonl
canonical_tags_raw.json
feature_vecs.npz
umap_*.html

# Spike outputs contain personal conversation titles/content; the curated log does not
spike_summary_tag_embed_cluster/*
!spike_summary_tag_embed_cluster/experiments.md
